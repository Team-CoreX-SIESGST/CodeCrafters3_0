# Flow Guardian — L40S Training Dependencies
# IMPORTANT: This requirements.txt targets the L40S server (CUDA 12.x).
# DO NOT use this file on the VivoBook demo laptop.
# The VivoBook uses os-observer-backend/requirements.txt (CPU + onnxruntime only).

# ── Deep Learning (CUDA 12.x build for L40S) ──────────────────────────────
torch==2.3.0+cu121
torchvision==0.18.0+cu121
--extra-index-url https://download.pytorch.org/whl/cu121

# ── ONNX export (must match onnxruntime version on VivoBook) ──────────────
onnx==1.16.0
onnxruntime==1.18.0          # CPU version — used for local export validation

# ── Data + numerics ───────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2          # confusion matrix, classification report only

# ── Visualization ─────────────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2

# ── Utilities ─────────────────────────────────────────────────────────────
tqdm==4.66.4                 # progress bars during training
